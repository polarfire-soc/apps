# Boot Mode configuration Softconsole Proj

At device power-up, MSS must boot directly from eNVM (Boot Mode 1) and execute HSS (ZSBL). 

Boot mode configuration is set in the device using this SoftConsole project.
